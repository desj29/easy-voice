services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - easy-stack-net
#    volumes:
#      - /export/hosts:/etc/nginx/certs
networks:
  easy-stack-net:
    external: true
